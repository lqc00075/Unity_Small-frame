using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TestResMgr : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //这个测试在对象池哪里测试过了 这里就不测试了
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
